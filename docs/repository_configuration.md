# Buckets Configuration
These values are set in the `config` file located in the top level `.buckets` directory.

| Name       | Default       | Comment                                       |
|------------|---------------|-----------------------------------------------|
| network.ntp_server | pool.ntp.org  | time server for correct timings of commits    |
| network.ip_check   | 8.8.8.8       | IP address to check for internet connectivity |
| network.url_check  | api.ipify.org | URL to check if DNS is working                |

