name: Build Debian Package

on:
  push:
    tags:
      - 'v*'
  pull_request:
    paths:
      - 'debian/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/debian-package.yml'
  workflow_dispatch:

jobs:
  build-deb:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
        toolchain: nightly-2025-01-15
        override: true
        components: rustfmt, clippy

    - name: Install build dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y dpkg-dev debhelper devscripts pkg-config libssl-dev build-essential fakeroot
        cargo install cargo-deb

    - name: Build Debian package
      run: |
        cargo deb --no-build --target x86_64-unknown-linux-gnu
        cargo build --release --target x86_64-unknown-linux-gnu
        
    - name: Upload Debian package artifacts
      uses: actions/upload-artifact@v4
      with:
        name: debian
        path: debian/
        
    - name: Test package installation
      run: |
        sudo dpkg -i debian-packages/buckets_*.deb
        buckets --version
        
    - name: Create Release (on tag push)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: debian-packages/buckets_*.deb
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}